#version 330 core

// Interpolated values from the vertex shaders
in vec2 Texturecords;

// Ouput data
out vec3 color;
 


// Values that stay constant for the whole mesh.
uniform sampler2D faceTexture;
void main(){

	color=((texture(faceTexture, Texturecords)).rgb);
}